{
  "pbtResults": {
    "2. Write bug condition exploration test": {
      "status": "passed",
      "failingExample": "18 failed | 5 passed (23) on `npm run test:integration`. Representative counterexamples: (C2) break-less clock-out left profiles.availability = 'available' — expected 'unavailable' — with rotation_state.whatsapp still pointing at the clocked-out agent aef23a7f-98c3-4e0a-8d3f-bc5c36d67740; (C3) break start from 'unavailable' wrote 'break', and time_clock_breaks.pre_break_queue_status does not exist so the prior value is unrecoverable; (C4) break end wrote 'available' over a deliberate 'unavailable'; (C6) POST /api/time-clock/breaks answered HTTP 201 for a customer_service employee while set_my_availability raised 'Agent permission required'; (C7) two concurrent clock-outs for one open entry BOTH answered HTTP 200, and 0 availability transitions were attributable where 1 is required; (C8) attributable availability records = 0 for My Status, break start, break end, clock-out, the daily reset (8 profiles genuinely changed, 0 events) and a user deletion (one audit_log row, action = 'user_deleted', which is not one of the eight 2.12 sources); public.availability_events does not exist; (C1) clock-in response carries no queue_status and no offers_join_sales_queues; (1.11) repeated clock-out answered HTTP 400 'Not clocked in.' and repeated break-end HTTP 400 'No active break to end.' where committed state is required. No hypothesized root cause was refuted.",
      "lastRunTimestamp": 1785871491034
    },
    "9.1 Verify the bug condition exploration test now passes": {
      "status": "failed",
      "failingExample": "npm run test:integration -- queue-status-bug-condition\n  4 failed | 19 passed (23)   [was 18 failed | 5 passed]\n\nRemaining failures, none of them a defect in the code this pass shipped:\n\n1) C8 > \"attributes a break start\"  — expected 1, received 0\n   c3 pre-state: queue = unavailable. attendance_break_start records\n   pre_break_queue_status = 'unavailable' and, per 2.4, does NOT write 'break'. The queue\n   value therefore does not move, apply_queue_status is never called, and 2.12 requires an\n   event only \"WHEN queue availability changes\". The test asserts one unconditionally.\n\n2) C8 > \"attributes a break end\"  — expected 1, received 0\n   c4 pre-state: queue = unavailable, pre_break_queue_status = 'unavailable'. Break end\n   restores exactly that value, so v_prev = p_new_status and apply_queue_status takes the\n   early return that is the designed idempotency point (design item 6). No change, no event.\n\n   1 and 2 are a test-versus-spec mismatch: Fix Checking guards the same assertion with\n   `IF changesQueueAvailability(F', X)`, and these two inputs are zero-delta. Byron's call\n   whether to guard the two assertions or to record zero-delta decisions as events.\n\n3) C8 > \"attributes the daily reset, once per genuinely changed profile\" — expected 8, got 0\n   DEFERRED: task 4.4 (recreate ensure_daily_availability_reset) is out of scope this pass.\n\n4) C8 > \"attributes a user deletion as an availability change\" — expected 1, received 0\n   DEFERRED: task 4.6 (recreate admin_deactivate_profile) is out of scope this pass.",
      "lastRunTimestamp": 1785873288893
    }
  },
  "executionHistory": {}
}